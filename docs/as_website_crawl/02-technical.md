## Технические нюансы и отличия режимов плагина

При переделке плагина под режим `website_crawl` важно учесть различия по сравнению с другими типами источников (online_drive и online_document), а также требования к формату выходных данных и механизмы удаления:

- **Типы плагинов-источников данных в Dify:** Начиная с версии 1.9.0, в Dify появились три типа дата-сорс плагинов: `web crawler` (веб-краулер), `online document` (онлайн-документ) и `online drive` (облачное хранилище)[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=Data%20Source%20Plugin%20Types). Текущий Git-плагин, судя по всему, реализован не как краулер, а либо как онлайн-документ, либо как инструмент. Например, **онлайн-документ плагин** предполагает, что сначала пользователь получает список страниц, затем выбирает одну для импорта[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=In%20the%20main%20logic%20code,to%20fetch%20the%20document%E2%80%99s%20content) (типичный пример – Notion или Confluence, где сначала отображается список страниц пространства, а потом загружается содержимое выбранной страницы). **Онлайн-драйв плагин** моделирует иерархическое хранилище: пользователь сначала видит список папок/файлов, может “проваливаться” в папки, и в итоге выбрать конкретный файл для загрузки[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=An%20online%20drive%20data%20source,adhere%20to%20the%20following%20specification). Такой подход малопригоден для целого репозитория с десятками/сотнями файлов, так как **требует ручного выбора каждого файла**. Режим же **`website_crawl`** работает иначе: плагин сразу сканирует всё сам и выдаёт результаты пачкой, без дополнительного выбора пользователем.
    
- **Наследование правильного базового класса:** При переходе на `website_crawl` плагин должен наследовать класс `WebsiteCrawlDatasource` (вместо, например, `OnlineDocumentDatasource`). В коде Python это будет примерно: `class GitRepoCrawler(WebsiteCrawlDatasource): ...`. Необходимо реализовать метод `_get_website_crawl(self, datasource_parameters)`, который **генерирует сообщения с данными**. Согласно документации, для веб-краулера логика такая: сначала можно выдать промежуточное сообщение о начале процесса (статус “processing”), а затем – итоговое сообщение со статусом “completed” и списком всех страниц[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=In%20the%20main%20logic%20code,completed)[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=crawl_res%20%3D%20WebSiteInfo%28web_info_list%3D%5B%5D%2C%20status%3D,yield%20self.create_crawl_message%28crawl_res). Dify позволяет вернуть данные через генератор (`yield`), что полезно для длительных задач – можно сообщить системе, что процесс идёт (чтобы не таймаутился), и по готовности отдать все собранные результаты.
    
- **Формат ответа `_get_website_crawl`:** Результат должен соответствовать схеме `WebSiteInfo`, включающей следующие основные поля: `web_info_list` (список объектов `WebSiteInfoDetail`), `status` (“processing” или “completed”), `total` (общее число страниц) и `completed` (сколько из них завершено)[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=crawl_res%20%3D%20WebSiteInfo%28web_info_list%3D%5B%5D%2C%20status%3D,yield%20self.create_crawl_message%28crawl_res)[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=content%3D,completed%20%3D%201). Каждый элемент `WebSiteInfoDetail` содержит:
    
    - `title` – заголовок или имя страницы (для файла можно использовать имя файла или относительный путь),
        
    - `source_url` – URL исходника (например, можно сформировать специальный URL вида `git://repo/path/to/file` или указать веб-ссылку на файл в Git-хостинге, либо просто путь – главное, чтобы он был уникальным для идентификации документа),
        
    - `description` – краткое описание (опционально, можно оставить пустым или использовать, например, последние коммит-сообщения, но это не обязательно),
        
    - `content` – собственно текстовое содержимое файла[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=crawl_res.status%20%3D%20,%29).
        
    
    Плагин должен **обеспечить текстовое содержание**. Для текстовых файлов (код, Markdown, текстовые документы) – контент считывается и возвращается как строка. Бинарные файлы (изображения, PDF и пр.) лучше либо игнорировать, либо, если очень нужно, конвертировать (но в рамках текстовой базы знаний Dify обычно не индексирует бинарные файлы напрямую). Можно настроить, чтобы плагин пропускал бинарные файлы или возвращал для них пустой контент с пометкой, но предпочтительнее исключить их на этапе краулинга (например, через фильтр расширений).
    
    > 🔹 **Важно:** Dify не требует от `website_crawl`-плагина разбивать контент на чанки – это делает сама платформа после получения документа. Поэтому `content` можно возвращать целиком (но с разумными ограничениями – очень большие файлы лучше обработать осторожно, например, применять limit или предупреждать пользователя о размере).
    
- **Механизм удаления (orphans):** Dify отслеживает соответствие между текущим списком документов от плагина и ранее импортированными документами. Технически, **если при синхронизации какой-то документ отсутствует в новом результате, он помечается “orphaned”** в базе знаний. Это означает, что его содержимое сохранено (можно даже увидеть в списке документов, отключенных от источника), но он больше не привязан к актуальному источнику и исключён из поиска (пока пользователь не решит вручную его вернуть, что обычно не делается)[docs.dify.ai](https://docs.dify.ai/en/use-dify/knowledge/create-knowledge/import-text-data/sync-from-notion#:~:text=Synchronizing%20Notion%20Data). Со стороны плагина **не требуется специального кода для удаления**; достаточно при повторном запуске просто **не возвращать** информацию о удалённых файлах. Поэтому реализация инкрементальной синхронизации сводится к тому, чтобы на каждую синхронизацию формировать актуальный полный список файлов. Dify сам вычислит “дифф”: новые добавит, существующие обновит (если их контент изменился – он перезагрузится), а пропавшие – отметит как удалённые.
    
    _Технический нюанс:_ чтобы Dify корректно сопоставлял документы между запусками, **необходимо сохранять постоянные идентификаторы**. Например, если сегодня файл `src/app.py` вернулся с `source_url = "src/app.py"`, то при следующей синхронизации тот же файл должен иметь точно такое же значение `source_url`. Тогда Dify поймёт, что это тот же документ и обновит его контент, а не создаст новый. Поэтому рекомендуется в качестве `source_url` (или иного идентификатора документа) использовать неизменяемые параметры – путь в репо +, возможно, хэш коммита или URL. На практике, для простоты можно использовать относительный путь внутри репозитория (он уникален для файла). В официальной схеме `WebSiteInfoDetail` поле `source_url` служит именно уникальным ключом страницы[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=title%3D,).
    
- **Отличия от `online_drive`:** В режиме онлайн-диска (`online_drive`) плагин работает совершенно иначе. Он реализует методы `_browse_files` и `_download_file`, возвращая структуру из папок и файлов интерактивно[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=An%20online%20drive%20data%20source,adhere%20to%20the%20following%20specification)[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=self%2C%20request%3A%20OnlineDriveBrowseFilesRequest%20%29%20,OnlineDriveBrowseFilesResponse). Выходной формат там – объект `file` с содержимым в двоичном виде (blob)[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=output_schema%3A%20type%3A%20object%20properties%3A%20file%3A,https%3A%2F%2Fdify.ai%2Fschemas%2Fv1%2Ffile.json). Git-репозиторий скорее можно было бы представить как иерархию (папки -> файлы), но этот режим **не поддерживает автоматическую загрузку всех файлов сразу**. Пользователю пришлось бы вручную кликать по каждой папке и файлу. Более того, режим `online_drive` выдаёт файл как единый blob, что не совсем оптимально для RAG-пайплайна (Dify ожидает текст для индексирования). Поэтому от `online_drive` мы отказываемся в пользу `website_crawl`.
    
- **Отличия от `online_document`:** В режиме онлайн-документа плагин должен сначала выдать список документов, из которых пользователь выбирает нужные, и затем по каждому выполняется отдельная загрузка контента[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=In%20the%20main%20logic%20code,to%20fetch%20the%20document%E2%80%99s%20content)[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=def%20_get_pages%28self%2C%20datasource_parameters%3A%20dict,page). Применительно к Git, это мог бы быть список файлов/директорий, но, опять же, это требует участия пользователя. Кроме того, `online_document` чаще используется для сервисов с API, где нецелесообразно за один вызов тянуть всё (например, Notion – выбирают только нужные страницы). В нашем случае цель – _полный_ охват данных, поэтому `website_crawl` предпочтительнее. Он позволит, например, как Firecrawl обходит все ссылки сайта, так и наш плагин обойдёт все файлы репозитория сам и вернёт их списком.
    
- **Оптимизация больших репозиториев:** Стоит учесть, что если репозиторий очень большой (сотни МБ текста), разовый краулинг может занять время и ресурсы. Dify плагин-демон обычно накладывает ограничения по времени ответа (если плагин не отвечает, может быть таймаут). Чтобы избежать таймаута, **можно выдавать промежуточные результаты батчами**. Документация советует: при крауле множества страниц устанавливайте `WebSiteInfo.status = "processing"` и используйте `yield` для выдачи каждой партии, затем в конце статус `"completed"`[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=In%20the%20main%20logic%20code,completed). Для Git это можно применять, если файлов очень много: например, можно каждые N файлов формировать промежуточный результат. Однако, возможно, проще сформировать один итоговый результат – ведь Dify всё равно не покажет документы пользователю, пока не завершится краул полностью (разве что в preview перед импортом). Поэтому допускается и единоразовая выдача, если укладываемся в приемлемое время. К примеру, Firecrawl при предпросмотре тоже возвращает список страниц сразу после завершения сканирования[docs.dify.ai](https://docs.dify.ai/versions/3-0-x/en/user-guide/knowledge-base/create-knowledge-and-upload-documents/import-content-data/sync-from-website#:~:text=2).
    
- **Учёт структуры репозитория:** Режим `website_crawl` не сохраняет древовидную структуру напрямую – он выдаёт плоский список страниц. Но мы можем кодом плагина включить путь в название или описание, чтобы было понятно, где лежал файл. Например, для файла `docs/readme.md` можно задать `title = "docs/readme.md"`. Впоследствии пользователь в Dify увидит этот путь в названии документа и сможет понять расположение. Также, если необходимо, можно включить в `description` что-то вроде “Git Repo: {repo_url}, Branch: {branch_name}” для контекста.
    
- **Права доступа и OAuth:** Один из нюансов текущего официального GitHub-плагина – он использует OAuth авторизацию к API GitHub. Судя по отзывам, это вызывало сложности (напр., ошибки redirect_uri)[github.com](https://github.com/langgenius/dify-official-plugins/issues/1805#:~:text=Github%20data%20source%20doesn%27t%20work,OAuth%20callback%20endpoint%20for). При переделке на прямое чтение репозитория (через git clone) **OAuth не понадобится**. Это упрощает архитектуру: вместо сложной OAuth-процедуры мы работаем с ключами (SSH или персональный токен). Нужно удостовериться, что Dify плагин может безопасно хранить эти креденшиалы. Обычно для Data Source плагинов аутентификация прописывается в `provider.yaml` – там можно определить либо API ключ, либо пару username/password, либо приватный ключ (например, RSA SSH) в виде multiline текста. Dify зашифрованно хранит эти данные и передаёт в `self.runtime.credentials` внутри плагина. Разработчику плагина придётся реализовать загрузку ключа из переменных окружения или файла, что можно предусмотреть. В любом случае, **технически отказ от OAuth в пользу прямого git-доступа устранит ряд ограничений** – например, можно будет обращаться к любому Git-совместимому хостингу (GitLab, Bitbucket, локальный Git-сервер), а не только к GitHub API.
    
- **Ограничения платформы:** Нужно учитывать версию Dify. Режим `website_crawl` поддерживается с Dify v1.9.0 и выше[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=Data%20source%20plugins%20are%20a,launch%20your%20data%20source%20plugin)[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=,supported%20Dify%20version%20as%20follows) – убедитесь, что `manifest.yaml` укажет `minimum_dify_version: 1.9.0` или выше. Кроме того, раз мы позиционируем плагин как источник данных (RAG), следует добавить тег `rag` в манифест, чтобы он отображался в соответствующей категории на Marketplace[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=minimum_dify_version%3A%201). Ещё нюанс: **поддержка автоматического перезапуска**. Dify, вероятно, не будет сам отслеживать изменения в репозитории (нет вебхуков Git). Поэтому если требуется именно автосинк (например, раз в сутки), это придётся реализовать либо кроном через API, либо не входить в текущий скоп. На стороне плагина никаких глобальных ограничений на частоту запусков нет – он может быть вызван по требованию пользователя многократно.
    

Резюмируя: технически переход на `website_crawl` означает **упрощение взаимодействия для пользователя** (всё загружается автоматически) ценой **усложнения логики внутри плагина** (нужно самому обходить и обновлять репозиторий). Однако Dify предоставляет нужные инструменты для реализации: веб-краулер плагин может вернуть многостраничный результат[docs.dify.ai](https://docs.dify.ai/en/develop-plugin/dev-guides-and-walkthroughs/datasource-plugin#:~:text=In%20the%20main%20logic%20code,completed), что соответствует нашим документам, а платформа позаботится о сравнении и обновлении документов между запусками.
