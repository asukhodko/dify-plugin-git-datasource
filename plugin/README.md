# Git Datasource Plugin for Dify

Dify плагин для импорта документов из Git репозиториев с поддержкой инкрементальной синхронизации.

## Возможности

- 📁 Импорт документов из любого Git репозитория
- 🔄 Инкрементальная синхронизация (только изменённые файлы)
- 🔐 Поддержка HTTPS, SSH и локальных репозиториев
- 🎯 Фильтрация по расширениям и поддиректориям
- 🔒 Безопасная работа с credentials

## Поддерживаемые источники

| Тип | Формат URL | Аутентификация |
|-----|------------|----------------|
| HTTPS (публичный) | `https://github.com/user/repo.git` | Не требуется |
| HTTPS (приватный) | `https://github.com/user/repo.git` | Access Token |
| SSH | `git@github.com:user/repo.git` | SSH Private Key |
| Локальный | `/path/to/repo` или `file:///path/to/repo` | Не требуется |

## Установка

### В Dify

1. Скачайте плагин из Dify Marketplace или соберите из исходников
2. Установите плагин в Dify
3. Настройте credentials для репозитория

### Сборка из исходников

```bash
# Клонировать репозиторий
git clone https://github.com/your-org/dify-plugin-git-datasource.git
cd dify-plugin-git-datasource/plugin

# Установить зависимости для разработки
make install-dev

# Собрать плагин (с тестами и линтером)
make package

# Или быстрая сборка без тестов
make package-fast
```

Пакет будет создан в `dist/git_datasource.difypkg`.

## Разработка

### Требования

- Python 3.12+
- Git 2.x+
- pip

### Настройка окружения разработки

```bash
# Перейти в директорию плагина
cd plugin

# Создать виртуальное окружение и установить зависимости
make install-dev

# Активировать окружение (опционально, для ручных команд)
source .venv/bin/activate

# Проверить что всё работает
make test
```

**Примечание:** Makefile автоматически создаёт виртуальное окружение в `.venv/` и использует его для всех команд.

### Структура проекта

```
plugin/
├── manifest.yaml           # Метаданные плагина
├── main.py                 # Точка входа
├── requirements.txt        # Production зависимости
├── requirements-dev.txt    # Dev зависимости
├── Makefile               # Команды разработки
├── _assets/
│   └── icon.svg           # Иконка плагина
├── provider/
│   ├── git_datasource.yaml # Конфигурация провайдера
│   └── git_datasource.py   # Валидация credentials
├── datasources/
│   ├── git_datasource.yaml # Конфигурация datasource
│   └── git_datasource.py   # Основная логика
├── utils/
│   ├── models.py          # Модели данных
│   ├── url_utils.py       # Работа с URL
│   ├── masking.py         # Маскирование credentials
│   ├── filtering.py       # Фильтрация файлов
│   ├── mime_utils.py      # MIME типы
│   └── storage_utils.py   # Ключи хранилища
├── git_client.py          # Git операции
└── tests/
    ├── property/          # Property-based тесты
    ├── unit/              # Unit тесты
    └── integration/       # Интеграционные тесты
```

### Команды разработки

```bash
# Установка
make install          # Production зависимости
make install-dev      # Все зависимости

# Тестирование
make test             # Все тесты
make test-property    # Property-based тесты
make test-unit        # Unit тесты
make test-cov         # Тесты с coverage

# Качество кода
make lint             # Проверка линтером
make format           # Форматирование кода

# Сборка
make package          # Собрать плагин (с тестами)
make package-fast     # Собрать плагин (без тестов)

# Очистка
make clean            # Удалить артефакты сборки
```

### Запуск тестов

```bash
# Все тесты
make test

# Только property-based тесты
make test-property

# С отчётом о покрытии
make test-cov
```

## Конфигурация

### Параметры подключения

| Параметр | Обязательный | Описание |
|----------|--------------|----------|
| `repo_url` | Да | URL репозитория |
| `branch` | Нет | Ветка (по умолчанию: main) |
| `access_token` | Нет | Токен для приватных HTTPS репозиториев |
| `ssh_private_key` | Нет | SSH ключ для SSH репозиториев |
| `subdir` | Нет | Поддиректория для импорта |
| `extensions` | Нет | Расширения файлов (например: `.md,.txt`) |

### Примеры

#### Публичный GitHub репозиторий

```yaml
repo_url: https://github.com/user/docs.git
branch: main
extensions: .md,.txt
```

#### Приватный GitLab репозиторий

```yaml
repo_url: https://gitlab.com/org/private-docs.git
branch: main
access_token: glpat-xxxxxxxxxxxx
subdir: docs/
extensions: .md
```

#### Локальный репозиторий

```yaml
repo_url: /home/user/my-docs
branch: main
```

## Инкрементальная синхронизация

Плагин отслеживает SHA последнего просмотренного коммита и при следующей синхронизации возвращает только изменённые файлы.

**Важно:** Инкрементальная синхронизация работает в режиме "best-effort":
- SHA обновляется после каждого успешного browse
- Если Dify вызывает browse без завершения индексации, некоторые файлы могут быть пропущены
- Для гарантированной синхронизации используйте внешнюю оркестрацию через Dify API

### Fallback на полную синхронизацию

Полная синхронизация выполняется автоматически когда:
- Первый запуск (нет сохранённого SHA)
- Force push (старый SHA недостижим)
- Слишком много коммитов между синхронизациями (>1000)
- Изменение конфигурации (branch, subdir, extensions)

## Обработка удалений

Когда файл удаляется из репозитория:
1. Он не появляется в результатах browse
2. Dify может пометить соответствующий документ как "orphaned"
3. Пользователь может удалить orphaned документы вручную

## Безопасность

- Credentials никогда не логируются
- URL с токенами маскируются в сообщениях об ошибках
- SSH ключи хранятся во временных файлах и удаляются после использования

## Лицензия

MIT
